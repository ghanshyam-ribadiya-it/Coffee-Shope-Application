server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ghanshyam-ribadiya-it/coffee-shop-app-cloud-config-server
          clone-on-start: true